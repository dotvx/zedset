# CLAUDE.md
> Zed IDE Configuration and Customizations

- These rules apply, whenever Claude or any other LLM is working here.
- **ALWAYS** prime yourself by doing the relevant research BEFORE:

## When working on Settings:

```zsh
Webfetch(https://zed.dev/docs/settings)
Read(settings.json)
```

## When working on Keybinds:

```zsh
Webfetch(https://zed.dev/docs/key-bindings)
Webfetch(https://zed.dev/docs/all-actions)
Read(keymap.json)
Read(tasks.json)
```

## When working on Themes:

```zsh
Webfetch(https://zed.dev/docs/themes)
Read(./themes/vx.desert.json)
```

## When working on Tasks:

```zsh
Webfetch(https://zed.dev/docs/tasks)
Read(tasks.json)
Read(./tasks/*.*)
Read(keymap.json)
```
